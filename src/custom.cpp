
#include "custom.h"

#include <vector>
#include <cmath>
#include <tuple>

using std::vector;
using std::tuple;
using std::tie;
using std::make_tuple;

Image custom(const Image& src_image, const Matrix<double> kernel) {
    Image dst_image;
    return dst_image;
}
